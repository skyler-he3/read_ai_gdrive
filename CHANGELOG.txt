## [2026-04-06] v2
- Switch from polling every 5 min to Drive Changes API for near-instant file detection
- Add weekly full scan every 7 days as a safety net
- State tracked in state.json (change token + last full scan timestamp)
- Scheduler interval reduced to 2 minutes (lightweight — only acts on actual changes)
- Move Log.txt: clean table format, only actual moves, appended chronologically
- Per-user logs: each teammate sees only their own files in their own folder

## [2026-04-06] v1
- Initial release
- Auto-move .docx meeting notes from shared 'Read AI Meeting Notes' folders to correct client subfolder
- Matches client name from filename (YYYY-MM-DD - <topic with client name>.docx)
- Creates Team Documents/Meeting Notes/ folder automatically if missing
- Skips duplicates using Google Drive MD5 checksum (no file download needed)
- Handles filename collisions by appending timestamp
- Writes Move Log.txt back into each teammate's Drive folder after every run
- Runs every 5 minutes via macOS launchd
- One-command setup via setup.sh
- Admin-controlled: teammates only need to share their folder, no installs required
